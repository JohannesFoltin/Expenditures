name: expenditures
description: How much did i spend? I actually don't want to know...
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  bloc: ^8.1.2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  equatable: ^2.0.3
  json_annotation: ^4.8.1
  meta: ^1.7.0
  uuid: ^4.5.1
  rxdart: ^0.28.0
  shared_preferences: ^2.0.15
  path_provider: ^2.1.4
  share_plus: ^10.0.3

dev_dependencies:
  build_runner: ^2.2.0
  bloc_test: ^9.1.2
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
  json_serializable: ^6.3.1
flutter:
  uses-material-design: true
  generate: true
